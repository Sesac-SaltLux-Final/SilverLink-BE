package com.aicc.silverlink.domain.call.repository;

import com.aicc.silverlink.domain.call.entity.EssentialQuestion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EssentialQuestionRepository extends JpaRepository<EssentialQuestion, Long> {
}
