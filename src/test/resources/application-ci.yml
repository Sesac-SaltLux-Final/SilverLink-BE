spring:
  config:
    import: optional:file:.env[.properties]

  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    # 💡 이 한 줄이 Dialect 에러를 해결해줄 겁니다.
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 💡 Redis 의존성 때문에 발생하는 Context 실패 방지
  data:
    redis:
      host: localhost
      port: 6379

security:
  jwt:
    secret: "ci_test_secret_please_use_a_long_random_string_1234567890_abcdefghijklmnopqrstuvwxyz"

api:
  welfare:
    service-key: "ci_dummy_api_key"

solapi:
  api-key: "ci_dummy_api_key"
  api-secret: "ci_dummy_api_secret"
  sender-number: "01000000000"